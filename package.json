{
  "name": "SwiftyActions",
  "version": "1.0.0",
  "description": "Sets up a Swift environment on linux",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "deploy": "rm -rf node_modules dist && npm install && npm run build && ncc build lib/main.js -o dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sersoft-gmbh/SwiftyActions.git"
  },
  "keywords": [
    "Swift",
    "Linux"
  ],
  "author": "ser.soft GmbH",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/sersoft-gmbh/SwiftyActions/issues"
  },
  "homepage": "https://github.com/sersoft-gmbh/SwiftyActions#readme",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/exec": "^1.0.3",
    "@actions/io": "^1.0.2",
    "@actions/tool-cache": "^1.3.3"
  },
  "devDependencies": {
    "@types/node": "^15.0.2"
  }
}
