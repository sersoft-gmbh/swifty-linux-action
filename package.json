{
  "name": "SwiftyActions",
  "version": "1.0.0",
  "description": "Sets up a Swift environment on linux",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "deploy": "rm -rf node_modules dist && npm install --only=prod && npm run build && ncc build lib/main.js -o dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sersoft-gmbh/SwiftyActions.git"
  },
  "keywords": [
    "Swift"
  ],
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/sersoft-gmbh/SwiftyActions/issues"
  },
  "homepage": "https://github.com/sersoft-gmbh/SwiftyActions#readme",
  "dependencies": {
    "@actions/core": "^1.2.3",
    "@actions/exec": "^1.0.3",
    "@actions/github": "^2.1.1",
    "@actions/tool-cache": "^1.3.3"
  }
}
